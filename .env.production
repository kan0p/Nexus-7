# Production build settings.
# Applies to `npm run build` and `npm run deploy` (GitHub Pages).
# The Dockerfile sets the same flags via ENV; this file covers local builds.
GENERATE_SOURCEMAP=false
DISABLE_ESLINT_PLUGIN=true
INLINE_RUNTIME_CHUNK=false
